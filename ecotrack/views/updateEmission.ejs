<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Update Emission</title>
    <link rel="stylesheet" type="text/css" href="updateEmission.css">
</head>
<body>
    <h1>Update Emission</h1>
    <form action="/emission/<%= emission._id %>" method="POST"> <!-- Adjust the form action and method as needed -->
        <input type="hidden" name="emissionId" value="<%= emission._id %>"> <!-- Hidden input to send emission ID -->

        <div class="form-group">
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" value="<%= emission.username %>" required>
        </div>
        <div class="form-group">
            <label for="date">Date:</label>
            <input type="date" id="date" name="date" value="<%= emission.date.toISOString().split('T')[0] %>" required>
        </div>
        <div class="form-group">
            <label for="emissions">Emissions:</label>
            <input type="text" id="emissions" name="emissions" value="<%= emission.emissions %>" required>
        </div>
        <div class="form-group">
            <label for="location">Location:</label>
            <input type="text" id="location" name="location" value="<%= emission.location %>">
        </div>
        <div class="form-group">
            <label for="source">Source:</label>
            <input type="text" id="source" name="source" value="<%= emission.source %>">
        </div>
        <div class="form-group">
            <button type="submit">Update Emission</button>
        </div>
    </form>
</body>
</html>
