<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Update Stock</title>
    <link rel="stylesheet" type="text/css" href="updateStock.css">
</head>
<body>
    <h1>Update Stock</h1>
    <form action="/stock/<%= stock._id %>" method="POST"> <!-- Adjust the form action and method as needed -->
        <input type="hidden" name="stockId" value="<%= stock._id %>"> <!-- Hidden input to send stock ID -->

        <div class="form-group">
            <label for="ticker">Ticker:</label>
            <input type="text" id="ticker" name="ticker" value="<%= stock.ticker %>" required>
        </div>
        <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= stock.name %>" required>
        </div>
        <div class="form-group">
            <label for="price">Price:</label>
            <input type="text" id="price" name="price" value="<%= stock.price %>" required>
        </div>
        <div class="form-group">
            <label for="volume">Volume:</label>
            <input type="text" id="volume" name="volume" value="<%= stock.volume %>" required>
        </div>
        <div class="form-group">
            <button type="submit">Update Stock</button>
        </div>
    </form>
</body>
</html>
