<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Product</title>
    <link rel="stylesheet" href="updateProduct.css">
</head>
<body>
    <h1>Update Product</h1>
    <form action="/product/<%= product._id %>" method="post"> <!-- Adjust the form action and method as needed -->
        <input type="hidden" name="productId" value="<%= product._id %>"> <!-- Hidden input to send product ID -->

        <div class="form-group">
            <label for="name">Product Name:</label>
            <input type="text" id="name" name="name" value="<%= product.name %>" required>
        </div>

        <div class="form-group">
            <label for="quantity">Quantity:</label>
            <input type="text" id="quantity" name="quantity" value="<%= product.quantity %>" required>
        </div>

        <div class="form-group">
            <label for="price">Price:</label>
            <input type="text" step="0.01" id="price" name="price" value="<%= product.price %>" required>
        </div>

        <div class="form-group">
            <label for="image">Image URL:</label>
            <input type="text" id="image" name="image" value="<%= product.image %>">
        </div>

        <!-- Add other fields for updating product details as needed -->
        <button type="submit">Update</button>
    </form>
    <!-- Optional: Add JavaScript for client-side validation or interactions -->
</body>
</html>
